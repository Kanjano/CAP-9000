# CAP 9000 - Python dependencies (local execution)
#
# Install:  pip install -r requirements.txt
#
# Runtime LLM is served by Ollama (https://ollama.com), not a Python package.
#   ollama pull qwen2.5-coder:3b   # default model (fast, ~1.9 GB)
#   ollama pull codellama          # fallback model

# --- Core backend (required) ---
flask==3.0.3
flask-cors==4.0.1
requests==2.32.3

# --- Optional: offline documentation downloader (download_docs.py) ---
beautifulsoup4==4.12.3

# NOTE: tensorflow / numpy / pandas / scikit-learn / langdetect were declared
# previously but never imported anywhere in the codebase -> removed.
# torch was only used by an unused prototype neural net (recursive_reasoning)
# and is now imported lazily; install it ONLY to experiment with that module:
#   pip install torch
